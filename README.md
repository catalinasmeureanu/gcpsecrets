# This script can be used to enable gcp secrets engine with a static account.

# Prerequised 

Vault server running with Vault Enterprise

# How to use it

Copy file vault-tester.json in the folder and run the script

$ chmod +x gcps.sh

$./gcps.sh